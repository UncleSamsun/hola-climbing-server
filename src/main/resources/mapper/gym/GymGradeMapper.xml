<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holaclimbing.server.domain.gym.mapper.GymGradeMapper">

    <sql id="columns">
        id, gym_id, label, difficulty_order, is_active AS active, created_at, updated_at
    </sql>

    <select id="findActiveByGymId" resultType="com.holaclimbing.server.domain.gym.domain.GymGrade">
        SELECT <include refid="columns"/>
        FROM gym_grades
        WHERE gym_id = #{gymId}
          AND is_active = TRUE
        ORDER BY difficulty_order, id
    </select>

    <select id="findActiveByGymAndId" resultType="com.holaclimbing.server.domain.gym.domain.GymGrade">
        SELECT <include refid="columns"/>
        FROM gym_grades
        WHERE gym_id = #{gymId}
          AND id = #{gradeId}
          AND is_active = TRUE
    </select>

</mapper>
